namespace :api,
  constraints: ApiConstraint.new(2.0) do
  resources :clusters,
    only: [:index, :show] do
    collection do
      get :repocheck
    end

    member do
      get :health
    end
  end
end
